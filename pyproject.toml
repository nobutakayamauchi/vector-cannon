[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "vector-cannon"
version = "0.2.0"
description = "Budget-aware multi-provider inference access and personal fire-control system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
authors = [
  { name = "Nobutaka Yamauchi" }
]
keywords = ["ai", "llm", "inference", "routing", "benchmark", "fire-control", "agents", "openai-compatible"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Topic :: Software Development :: Quality Assurance",
]

[project.scripts]
vector-cannon = "vector_cannon.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
