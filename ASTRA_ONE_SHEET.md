# ASTRA ONE SHEET — vector-cannon

- **Repository:** `vector-cannon`
- **Visibility:** public
- **Initial status hint:** `ACTIVE`
- **Initial role hint:** Reserve / escalation fire-control
- **Local source of truth:** `README.md` and canonical docs/config
- **Do not assume:** Vector Cannon should be on the normal path when direct FDE execution is sufficient

## Astra review contract
Verify its current raison d'être as continuity/escalation/reserve fire-control: preserve decisive capability, choose necessary-and-sufficient power, avoid MAX-by-default, route around quota/provider/tool/cost failures, and remain adapter-driven rather than model-hardcoded.

Check overlap with `LLM-Cost-Router` conceptually without assuming either must survive standalone. Final disposition must preserve outcome, not architecture.

Allowed verdicts: `KEEP_CORE`, `KEEP_FALLBACK`, `ABSORB`, `REPLACE_EXTERNAL`, `FREEZE`, `DELETE`, `PRODUCT`.
